# Changelog

このファイルの形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [1.3.1] - 2026-09-25

外部レビューと、修正ごとの再検査を重ねて見つかった不具合を直しました。

### 変更

- **画面**: クリアとエクスポートのボタンを、どの画面でも一覧・ログのすぐ上に左寄せで並べました（FTP / TFTP / SFTP サーバーにもエクスポートを追加）
- **画面**: 窓を狭めてもボタンが隠れないようにしました（SNMP は最小幅 1160px → 376px）。SNMP の GET / WALK はポート欄をホストの下へ移しました
- **保存**: ログ記録の開始とエクスポートは、前回保存したフォルダから開きます
- **保存**: エクスポートは、選んだ種類に合わせて拡張子が変わります（Syslog に CSV を追加）。付け替えた先にファイルがあるときは上書きを確認します
- 端末の範囲選択は、ドラッグした時点でクリップボードへコピー。右クリックは貼り付け（改行や制御文字を含む内容は、送る前に確認）
- 端末へのテキストのドラッグ＆ドロップと、編集メニューの「ペースト」（Ctrl+Shift+V）を廃止
- キープアライブとマクロは、接続先リストで機器を右クリック →「ツール」へ移動（未接続・接続先が違う項目では選べません）
- タブを開いている機器は、名前と接続先（ホスト・ポート・プロトコル）の変更、削除ができません
- 打ちかけの入力があるあいだは、キープアライブを送りません
- SNMP の GET / WALK を途中で止められます（そこまでの結果を「途中まで」として表示・書き出し）
- NetBelt を 2 つ同時に起動できなくなりました（2 つ目は動いている窓を前に出して終了）
- 保存されたパスワードを復号できない機器へは接続しません（起動時にも知らせます）
- `known_hosts` に読めない行があり、名前欄がその機器の接続先と完全に一致するときは、その機器への接続を断ります（ワイルドカードの行やハッシュ化した名前の壊れた行は照合せず、警告だけ出して接続します）。保存済みと違う鍵が来たときは上書きせず中止します
- SFTP クライアント: 上書きの確認を経ていない送信は既存ファイルを置き換えません。同じ保存先への二重ダウンロードを断ります。リンクの権限変更はリンク先の現在値を初期値にします
- 内蔵 FTP / TFTP / SFTP サーバ: 同じ名前へ同時にアップロードされたときは、後から来た方を断ります
- 同じ名前の共通マクロ（プリセット）が複数あるときは、その名前のマクロの実行・編集・削除を断ります（起動時にも知らせます）
- 自動更新: インストール先を排他してから書き換え、展開の直前に ZIP のハッシュを照合します
- ログ記録中のダイアログは、アプリが書いたバイト数を表示します（記録の開始・停止のメッセージは廃止）

### 修正

**端末・ログ**

- 長い出力の途中で上へスクロールしても、一番下へ戻されていた
- `show tech-support` のような大量出力で端末が固まっていた
- 画面消去や行の挿入・削除を含む出力で、数秒〜十数秒固まることがあった
- 受信が描画より速いと、描いていない分が際限なく溜まっていた
- コピーと全ログ保存で、無関係な行が 1 行に繋がる・1 行が 2 行に割れることがあった
- 窓の大きさや文字サイズを変えると、受信済みの文字が消えることがあった
- 更新のために終了すると、記録中のログが閉じられず受信分が失われていた
- 閉じている途中に、更新の知らせなどのダイアログが開くことがあった
- 大きな貼り付けの送信が、長さに対して極端に遅くなっていた
- SSH / Telnet で、受け取りの遅い機器へ大きく貼り付けると、途中でセッションが切れていた

**接続・設定**

- 起動時に `Default` グループを補う保存が、Windows では必ず失敗していた
- 設定ファイルの一部が壊れているだけで、設定全体が初期状態に戻っていた
- 同じ名前の機器が 2 台あると、編集・削除・移動で別の 1 台が書き換わる・消えることがあった
- グループの変更が保存に失敗しても、終了時に保存されていた
- NetBelt を 2 つ起動していると、`known_hosts` の保存がぶつかって鍵が残らないことがあった
- `known_hosts` にハッシュ化した名前の壊れた行が 1 行あるだけで、どの機器にも接続できなくなっていた
- 設定のポート番号が文字列（`"22"` など）だと、SSH で保存済みのホスト鍵と照合されず、別の鍵を受け入れていた（Telnet は繋がらなかった）
- 同じ名前の機器が別のグループにあると、接続した機器とは別のグループの自動実行コマンドを送っていた
- 同じ名前の共通マクロが複数あると、選んだものと別のマクロが送られる・まとめて削除されることがあった
- コンソール接続のボーレート変更をポートが拒んでも、表示と再接続用の設定が戻らなかった

**内蔵サーバ**

- TFTP: 機器が転送を打ち切っても再送を続けていた
- Syslog（TCP）: 終端の無い最後の 1 行が届かないことがあった。保持件数の設定が壊れていると受信できなかった
- FTP: アップロードの進捗が出ていなかった。終わった転送の名前で進捗が出ることがあった
- FTP / SFTP: 同じ名前へ同時にアップロードされると、どちらも成功と出るのに中身が混ざっていた
- TFTP: 大きさ（tsize）が極端な要求で画面にエラーが出ていた。停止の待ち時間が転送の本数ぶん長くなっていた
- Syslog とサーバのログ: 書き出したテキストで、特殊な改行文字によって 1 行が割れることがあった

**SNMP・MIB**

- 別ベンダーの MIB にある同名の定義を親として借りて、Trap に他社の名前が付くことがあった（一部の形は未対応）
- `IMPORTS` などの語が定義の名前として登録されることがあった
- BOM 付きで連結した MIB で、モジュールの境目を取り違えていた
- `mibs` フォルダのファイルが読めない・消えたときに、MIB を 1 件も読み込めないことがあった
- 書き出した CSV に対象ホストが入っていなかった

**SFTP クライアント**

- 置き換えに失敗したとき、最終名も一時名も機器から消えることがあった
- 時間切れや切断のあとも「接続中」の表示が残っていた。同じ知らせが 2 回出ることがあった
- リンクを確かめている最中に SFTP が切れると、「接続中」のまま残っていた

**自動更新**

- インストール先へ書き込めないときに「別の更新が進行中です」と出ていた
- 更新の残骸（適用用の写し・控え・一時名の実行ファイル）が片付かず溜まっていた
- 2 つの NetBelt が同じ更新を保存すると、「ダウンロード完了」と出たのに適用できないことがあった
- TEMP のパスに `[` `]` があると、更新が毎回失敗していた（1.3.0 以前からの更新では、その 1 回だけ手で入れ替えが必要です。README を参照）

> 1.2.0 以前から更新する場合は、更新を当てるのが旧版の `updater.bat` のため、
> 途中で英語のエラーが出ることがあります。詳しくは README の自動更新の項を
> 参照してください（NetBelt を終了してから当ててください）。

## [1.3.0] - 2026-09-17

コード全体を対象にした外部レビューを 3 回（104 件、76 件、6 件）行い、
見つかった不具合をすべて直しました。3 回目のレビューで影響の小さかった
7 件は、直すと壊す危険のほうが大きいと判断してそのままにしています
（残る制限としてコードに記録しました）。

4 回目のレビューでは、3 回目の修正そのものが持ち込んだ不具合が 2 件
見つかりました（下の「重い不具合」の 1 つ目と 2 つ目）。

1.2.0 から更新する場合は、この節の最後にある「8 月 28 日までに入っていた
変更」の冒頭も読んでください。ファイアウォールの扱いが変わっています。

### 変更
- **コンソール接続のボーレートを変えると、開いているタブにその場で効く
  ようになりました。** 機器側で `speed 115200` などを実行したあと、
  機器一覧の右クリックからボーレートを合わせれば、タブを閉じて繋ぎ直さずに
  そのまま操作を続けられます。接続もさかのぼれる履歴もそのままです。
  アダプタがその速度に対応していないなどで変更できなかったときは、
  接続を元のボーレートのまま残し、ステータスバーでお知らせします。

### 修正（重い不具合）
- **同じ名前の機器が別のグループにあると、接続した機器に別グループの
  自動実行コマンドが送られていた。** 所属の判定を機器名だけで行っていた
  ためです。機器名は全グループを通して一意になり、重複する名前の追加・
  複製・改名は理由を示して断ります。既に重複している設定は、片方の
  名前を変えてください。
- **切断したあともマクロが止まらず、同じ名前で再接続した先へ残りの
  コマンドが送られていた。** キープアライブも同じです。切断で止めます。
  接続直後に予約される自動実行コマンドも、待っている間に切断・再接続
  されていたら始めません。再接続後もキープアライブは自動では再開
  しないので、必要ならマクロダイアログから入れ直してください。
- **機器を編集して保存に失敗すると、登録が消える／新旧 2 件になる
  ことがあった。** 削除と追加の 2 回に分けて保存していたためです。
  1 回の保存で差し替え、失敗したら何も変えません。
- **SFTP で機器を切り替えた直後、前の機器の一覧が残っていた。** その
  間に削除を承認すると、新しい機器の同名ファイルを消していました。
  切り替えた時点で一覧を空にします。
- **SFTP の右クリックメニューを開いている間にディレクトリが変わると、
  別の場所のファイルを操作していた。** メニューを開いた時点の機器と
  場所に固定します。
- **SFTP の転送が失敗すると、既存のファイルが 0 バイトや途中までの
  内容に置き換わっていた。** ダウンロードもアップロードも一時的な名前へ
  転送し、成功してから置き換えます。ダウンロードは失敗しても元の
  ファイルが残ります。アップロードは、途中までの内容が本来の名前で
  残ることは無くなりました（置き換えに失敗した場合は、転送済みの内容が
  機器側の一時名 `.<名前>.netbelt-part` に残り、その名前を表示します。
  切断時も同じ名前の一時ファイルが機器側に残ることがあります）。
  新規作成が許されず上書きだけ許されるディレクトリでは、一時名を作れない
  ため転送できなくなります。
- **ログ記録の書き込みに失敗しても「記録中」のままだった。** ディスク
  満杯や共有フォルダの切断で起きます。記録を止めて警告を出します。
  記録は失敗する前の行までです。
- **SSH の既知ホスト鍵ファイル（known_hosts）を読めないとき、黙って
  検証なしで接続していた。** 鍵が変わった機器にも気づかずにパスワードを
  送る状態でした。読めない場合は接続を中止し（壊れた行が 1 つある
  だけでも読めなくなります。該当行の修正・削除かファイルの退避を案内
  します）、新しい鍵を保存できない場合は画面に警告を出します。
- **FTP サーバのユーザー名に anonymous を使うと、任意のパスワードで
  全権限のログインが通っていた。** ライブラリがこの名前を匿名として
  特別扱いするためです。通常ユーザーとしては登録できなくします。
  匿名を使う場合は「匿名を許可」を使ってください。
- **TFTP のアップロードで、保存の最後の書き込みが失敗しても「成功」と
  返していた。** ファイルを閉じてから応答するようにし、失敗したら機器へ
  エラーを返して記録に残します。
- **SFTP サーバで、リンク（ジャンクション）を削除・改名するとリンク先の
  本体が消えたり改名されたりしていた。** リンク自体に作用します。
  あわせて、ルートそのものや `.`／`..` で終わるパスの削除・改名は断ります。
- **SNMP の結果を保存するとき、対象ホストに保存時の入力欄の値を書いて
  いた。** 取得したあとにホストを書き換えて保存すると別の機器の記録に
  なっていました。取得したときのホストを書きます。「既に操作が実行中
  です」で断られた要求のホストは記録しません。
- **SNMPv3 の Trap 受信で、登録したユーザより弱いセキュリティレベルの
  通知を受け入れていた。** authPriv で登録したユーザ名に、鍵を付けない
  通知が届いていました。登録したレベル未満は捨て、一度だけ知らせます。
- **アプリが、操作と関係のない場所で突然落ちることがあった。**
  SNMP の受信スレッドからの通知を中継する書き方に誤りがあり、
  通知の届け先が片付けられたあとも配送待ちが残っていました。
  そのあとで画面が何かを処理した瞬間に、解放済みの領域へ届きます。
  落ちる場所が毎回変わるのはこのためです。中継の書き方を直しました。
  一覧の内部データが表示より先に片付けられる経路も塞いでいます。
- **FTP サーバの停止を、待ち受けとは別のスレッドから行っていた。**
  ライブラリが想定していない使い方で、停止の直後に待ち受け側が
  エラーを出していました。あわせて、サーバが待ち受けの仕組みを
  プロセス全体で共有していたのをサーバごとに分けました。

- **SFTP サーバで、公開したフォルダの外にあるファイルを削除・改名
  できた。** `C:名前` のようなドライブ付きの名前を送られると、Windows の
  パス結合が公開フォルダ側を捨てるためです。届く先はアプリを置いた
  フォルダ（設定ファイルやホスト鍵の場所）でした。この形の名前は
  受け付けません。
- **SFTP でファイルを送ったとき、置き換えの応答が返ってこないと、
  送ったファイルと元のファイルの両方が消えることがあった。** 機器側では
  置き換えが済んでいるのに、応答だけ期限切れになった場合です。後片付けが
  置き換わったばかりのファイルを消していました。期限切れのときは
  どちらの名前にも触れず、機器側で確かめる一時名をお知らせします。
- **自動更新が、アプリの隣にある `backup_netbelt_*` という名前の
  フォルダを、確認も通知もなく削除していた。** NetBelt はこの名前の
  フォルダを作りません。つまりその名前があれば利用者が置いたもので、
  機器のコンフィグの退避先かもしれません。更新スクリプトは、アプリの
  隣にあるフォルダを名前にかかわらず削除しません。
- **SNMP の結果や Trap の保存先に、端末がログ記録中のファイルを選ぶと、
  記録済みの内容が消えた。** 端末どうしの上書きは防いでいましたが、その
  検査を SNMP の書き出しが通っていませんでした。記録中のファイルが
  選ばれたら、どの機器が使っているかを示して断ります。

- **未適用の更新ファイルを調べている最中にそのファイルが消えると、
  アプリが起動しなかった。** NetBelt を二重に起動していて、片方が更新を
  適用し終えた瞬間などに起きます。調べられなかった候補は飛ばして起動を
  続けます。

### 修正（細かい不具合）
- **接続先リストの「切断」ボタンを押しても何も起きなかった。** 押した機器を
  切断します。タブは開いたままなので、そのまま再接続できます。
- **ツール領域を境界のドラッグで幅ゼロまで畳むと、表示ボタンでも戻せなく
  なっていた。** 戻せるようにしました。「表示/非表示」で隠したときは、
  これまでどおり隠す前の幅に戻ります。
- **タブをドラッグして並べ替えている途中で下へ引き剥がすと、掴んでいない
  タブが切り離されることがあった。** 掴んでいるタブが切り離されます。
- **SFTP サーバを開始した直後にアプリを閉じると、サーバが止まらないことが
  あった。** 待ち受けのスレッドが動いていれば、待ち受けに入る前でも
  止めます。
- **切断した接続や SFTP の抜け殻が、アプリを閉じるまで残っていた。**
  切断のたびに解放します。SFTP に対応していない機器へ接続したときも
  残らなくなりました。
- **画面消去（ESC[1J）で画面全体が消えるとき、消える前の内容が履歴に
  残らなかった。** 履歴へ送ってから消します。コピーやログ保存で
  失われなくなりました。
- **カーソル位置の保存と復元（DECSC/DECRC）で、文字セットの選択が戻らな
  かった。** 一緒に戻します。罫線を使う画面で崩れなくなります。
- **代替画面へ入り直すと、前回の内容が消えていた。** 47 での出入りでは
  内容を保ちます（1047 は仕様どおり、抜けるときに消します）。
  47 と 1047 では、入るときにカーソルもその場に残ります。
- **消去命令（ED/EL）に定義の無い番号を渡されると、画面全体（EL なら
  行全体）を消していた。** 定義の無い番号は無視します。
- **Telnet で、大きすぎるサブネゴシエーションを捨てたあと、画面に何も
  出なくなることがあった。** 捨てる直前のデータが 1 バイトの `IAC` で
  終わっていると、続きの受信の先頭にある終端を見つけられず、以後
  受け取ったものを全部捨て続けていました。再接続するまで戻りません。
  その 1 バイトを持ち越して、終端を見つけられるようにしました。
- **GitHub のトークンが `config.json` に平文で保存されていた。** 機器の
  パスワードと同じように暗号化します。
- **`DPAPI:` で始まるパスワードが、暗号化されずに平文で保存されていた。**
  暗号化済みの値と同じ形に見えるためです。暗号化できるものは暗号化し、
  それでも見分けが付かない形（`DPAPI:` のあとが英数字だけなど）は、
  機器の登録画面で保存前に確認します。FTP／SFTP サーバのパスワード欄では
  確認は出ず、平文で保存したうえでサーバの開始を断ります。別の文字から
  始まるパスワードをお使いください。
- **SNMP の結果を CSV で保存すると、Excel で文字化けしていた。** BOM を
  付けて保存します。
- **SFTP のパーミッション変更ダイアログが、setuid・setgid・スティッキー
  ビットを黙って落としていた。** 保ちます。
- **2GiB を超える転送で、FTP と TFTP の進捗表示が壊れていた。** 正しく
  表示します。
- **Syslog で、CRLF が受信の切れ目にまたがると 1 行として数えられず、
  行の長さ制限に早く当たっていた。** またがっても正しく数えます。
- **Syslog サーバの停止が、TCP の接続を処理しているスレッドを待たずに
  完了と表示していた。** 待ってから知らせます。
- **SFTP サーバが、サイズや更新時刻の変更要求に「成功」と答えるだけで何も
  していなかった。** 実際に適用します。
- **実行ファイルの置き場所の名前に `%環境変数%` が含まれると、
  ファイアウォールの許可登録が展開後の別のパスに対して行われていた。**
  管理者で実行していないときだけ起きます。パスに `%` が含まれる場合は、
  展開されるかどうかに関わらず登録せず、理由を知らせます（管理者で
  実行しているときはこれまでどおり登録します）。
- **README のファイアウォールの説明が、起動時に自動で許可を追加すると
  読めた。** 実際には各パネルの「ファイアウォールで許可」を押したときだけ
  追加します。説明を実際の動作に合わせました。
- **FTP の転送が途中で切れても、履歴の行が「転送中」のまま残っていた。**
  「中断」と表示します。
- **更新のダウンロードが、リリースに添付された別の zip を掴むことが
  あった。** 配布物の名前で選びます。
- **更新の途中で中断した残骸があると、実行ファイルを含まない zip でも
  「更新完了」と表示し、動いている実行ファイルを壊していた。** 残骸を
  新しい実行ファイルと取り違えないようにしました。
- **更新の作業場所の名前が、同じころに始まった別の処理とぶつかって
  いた。** 名前を `%RANDOM%` 任せにしていましたが、この値は処理の
  開始時刻から作られるため、同じ秒に始まると同じ番号を引きます。
  作業場所を 1 つずつ確保し、終わったら丸ごと片付けます。
- **失敗した更新の残骸が一時フォルダに溜まり、次の更新で中身が
  紛れ込むことがあった。** 残骸と同じ名前を引くと、作業場所を
  作れないまま処理が進み、残骸のファイルも一緒にインストール先へ
  コピーされていました。しかも「更新完了」と表示されます。

### 修正（端末の表示と記録）
- **画面の一部だけを使うスクロール領域の指定で、下端に 0 を書かれると
  狭い領域が残ったままになっていた。** 0 は「既定値」の意味なので、
  画面いっぱいに戻します。
- **端末のリセット（RIS）で、画面に出ていた内容が履歴に残らず消えて
  いた。** 画面消去（ED 2）と同じように履歴へ送ってから消します。
- **折り返しの印が行を書き直しても残り、履歴やログで別々の行が
  つながって見えることがあった。** その行に文字を書いた時点で外します。
- **画面を広げたあと、行の途中に文字を差し込むと行末の文字が
  消えていた。** 桁数に余裕があるかを見てから押し出します。
- **端末が解釈しない制御シーケンスを、途中まで一致しただけで既知の
  命令として実行していた。** 画面が突然消えたり代替画面へ切り替わったり
  する原因でした。解釈しないものは無視します。
- **全角文字・結合文字・絵文字を 1 桁として数えていたため、機器側の
  桁揃えが崩れていた。** 全角は 2 桁、結合文字と幅ゼロの文字は 0 桁と
  して数えます。カーソルの表示位置も桁ではなく画面のセルで数えます。
  曖昧幅の文字は 1 桁のまま、絵文字の合字は分けて数える、幅ゼロの
  文字が行頭に来たときは捨てる、という制限は残ります。
- **マクロとキープアライブの送信が、貼り付け中のかたまりの間に
  割り込んでいた。** 端末の送信待ち行列を通すようにしました。
- **記録中のファイルを、別のタブの記録や「全ログ保存」が上書きして
  いた。** 使用中のファイルは、どの機器が使っているかを示して断ります。
- **「全ログ保存」を取り消したり失敗したりすると、選んだ既存ファイルが
  空になっていた。** 一時ファイルへ書き切ってから置き換えます。
  取り消したときは保存先に触れません。
- **実行中のコマンドリストを止める方法が画面に無かった。** 実行中は
  右クリックメニューに「マクロ停止」が出ます。
- **端末の表示内容に上限が無く、長時間つないだままだとメモリを
  使い続けていた。** 表示は 20000 行までにし、元に戻す履歴も持ちません。
  「全ログ保存」は画面に残っている分を保存するので、上限を超えた
  ぶんは保存できません。保存するときにその旨を知らせます。全部を
  残したい場合は「ログ記録」を使ってください。
- **タブを閉じても端末が解放されず、閉じるたびにメモリが増えていた。**
  閉じた時点で解放します。
- **「全ログ保存」の進捗ダイアログを Esc や × で閉じても、保存処理が
  裏で走り続けていた。** 閉じると止まります。
- **切断したタブへ機器ツリーから接続し直すと、キーボード入力を
  受け付けないことがあった。** Enter での再接続では起きず、接続や
  ダブルクリックでタブを使い回したときだけ再接続待ちの状態が
  残っていました。タブを作り直す時点と、再接続に失敗した時点の
  両方で解除します。
- **作業フォルダに `logs` を作れないと、ログ保存のダイアログすら
  出なかった。** ダイアログは必ず出し、作れない場合はホームフォルダを
  既定にします。

- **表示できる行数の上限に達したあと端末の大きさを変えると、さかのぼれる
  履歴に同じ行の重複と抜けが積み上がった。** 画面を描き直す起点を、Qt が
  先頭の行を捨てたあとも古いまま使っていたためです。縦に 6 往復させると
  重複 443 行・断裂 12 か所になり、その内容がそのまま全ログ保存にも
  出ていました。
- **上限で古い行が捨てられたあと画面を少し縮めるだけで、全ログ保存の
  「古いログは含まれません」という警告が出なくなった。** 行数がその時点で
  上限を下回るかどうかだけを見ていたためです。一度でも捨てたことを
  覚えておき、欠けたログを完全なものとして保存しません。
- **改行のない出力を送り続けると、表示のメモリが上限に関係なく増え
  続けた。** 折り返した行を 1 つのかたまりとして連結していたため、
  行数の上限が効きませんでした。かたまりの長さにも上限を設けます。
- **マクロを止めても、送信待ちのコマンドが機器へ送られていた。**
  「マクロ停止」はタイマーと管理情報を消すだけで、端末の送信列へ渡した
  ぶんはそのまま残っていました。停止で取り消します。
- **マクロの送信中に切断されると、内部で例外が出ていた。** 送信が
  その場で失敗を通知し、後片付けが先に進むためです。
- **稼働中のキープアライブの間隔が、実際の値にかかわらず「60 秒」と
  表示されていた。** ダイアログが稼働状態しか受け取っていなかった
  ためです。10 秒で始めて開き直すと 60 秒と表示され、そのまま止めて
  始め直すと本当に 60 秒になっていました。
- **ログ記録の経過時間が 24 時間ごとに 0 へ戻っていた。**
- **全ログ保存の取り消しが、最後の書き込みのあとでは効かなかった。**
  取り消しても保存先が置き換わっていました。
- **全ログ保存の取り消しが、書き込みの終わりを画面を止めたまま待って
  いた。** 共有フォルダの切断などで戻らない場合、全タブの操作と受信表示が
  止まりました。待つ時間に上限を設け、越えたら状況をお知らせします。
- **ログファイルを閉じるのに失敗すると「記録中」の表示が残り、記録を
  始め直せなかった。**
- **記録中ダイアログと右クリックメニューが、閉じたあとも積み上がって
  いた。** 記録を 3 回始めて止めると 3 個、右クリック 5 回で 25 個の
  項目が残っていました。閉じた時点で破棄します。
- **端末エミュレータの細かい取りこぼしを直しました。** 全角文字の前半で
  画面を消すと履歴へ退避されない、画面先頭の行削除が履歴へ送られずに
  捨てられる、代替画面から戻ると文字集合が元に戻らない、カーソルの
  保存・復元で折り返し待ちが失われる、代替画面とメイン画面がカーソルの
  保存場所を共有していた、CSI E / F がスクロール範囲を越える、
  行の挿入・削除のあとカーソルが行頭へ戻らない、行頭までの消去で
  折り返し印が残る、自動折り返しを切っているとき結合文字が 1 つ左の
  文字に付く、代替画面へ入るときに不要なカーソル移動が起きる。

- **文字の削除・挿入で行が空になっても、折り返しの印が残っていた。**
  行頭までの消去では直していましたが、同じことが別の操作からも起きました。
  履歴とコピーで空行が次の行と繋がります。
- **全角文字の後半桁にカーソルがある状態で「カーソルから画面末尾まで」を
  消すと、画面が空になるのに履歴へ退避されなかった。**
- **マクロを止めたとき、送信中だった 1 行が途中までしか機器へ届かず、
  中途半端な文字列が入力行に残った。** 送信中の 1 行は送り切ってから
  止めます。
- **右クリックメニューが、閉じたあとも画面の子として積み上がっていた。**
  端末では直しましたが、機器一覧・SFTP・Syslog・メインウィンドウでも
  同じことが起きていました。

### 修正（接続・機器の登録・SFTP・SNMP）
- **日本語の出力が受信の切れ目にまたがると、文字化けして表示されて
  いた。** SSH・シリアル・Telnet のいずれも、続きが届くまで待ってから
  文字に直します。
- **別の Windows アカウントや別の PC で作った設定を読むと、復号できない
  パスワードをそのまま FTP／SFTP サーバの認証情報として登録していた。**
  暗号文のままログインが通る状態でした。復号できない場合はサーバを
  開始せず、パスワードの入れ直しを案内します。
- **シリアル接続を開いている最中に切断すると、COM ポートを掴んだまま
  残っていた。** 次の接続がアクセス拒否になり、再起動するまで直りません
  でした。開いたポートを閉じてから終わります。
- **シリアルの送信が画面を止めていた。** 300 bps では 512 文字の
  貼り付けで約 17 秒固まっていました。送信は別スレッドで行います。
- **機器名やホストが欠けた `config.json` を読むと、アプリが起動しなく
  なっていた。** 壊れた項目だけを外して起動し、控えを取った旨を知らせ
  ます。同じファイルの正常な機器はそのまま使えます。名前の無い
  グループは「(名前なし)」として表示し、グループとして読めない項目は
  外します。
- **機器を編集すると、マクロや通信速度など画面に出ていない設定が
  消えていた。** 編集していない項目はそのまま残します。
- **接続し直したあと、前の接続の切断・エラー通知が新しい接続の表示を
  書き換えていた。** 通知がどの接続のものかを見て、古いものは捨てます。
- **SSH 接続の直後、SFTP の準備が終わるまで画面が固まっていた。**
  別スレッドで行います。タブを閉じたあとに準備が終わった場合は
  破棄します。切断にならない接続エラーのときも、閉じた接続を
  抱えた SFTP を破棄してパネルを空にします。
- **ポート確認の画面を開いたままアプリを閉じると、終了しなかった。**
  一緒に閉じます。
- **設定ダイアログで取り消しても、保存に失敗した内容が画面に残って
  いた。** 失敗したらメモリ上の設定も元に戻します。
- **接続方法がコンソールのとき、ポート欄に数字以外があると「予期しない
  エラー」で落ちていた。** コンソールではポート欄を使いません。
- **ホームタブの名前と同じ「ホーム」を機器名に付けられた。** その
  機器のタブは閉じられず、ログ保存・記録・マクロ設定もホームタブ
  扱いで断られていました。予約語として断ります。手で編集した設定や
  以前のバージョンで作った設定に入っている場合は、読み込み時に
  外して知らせます。
- **SFTP のアップロードで、上書き確認が古い一覧を見ていたため、
  確認なしに既存ファイルを置き換えることがあった。** 送信の直前に
  機器側を確認し、確認を取っていなければ送りません。
- **SFTP の通信が無反応になると、画面が固まったままになっていた。**
  30 秒で打ち切り、何秒応答が無かったかを知らせて切断します。
  接続し直してください。
- **パーミッションを返さない機器の一覧が、1 件でも混ざると全部
  表示されなかった。** 不明として表示します。
- **ポート確認で、使用中のポートを「空いている」と表示することが
  あった。** 排他的な確保で試します。
- **ポート確認が `netstat` の行を文字の一致だけで拾っていたため、
  80 番を調べると 8080 番などが混ざっていた。** 番号とプロトコル、
  待ち受け状態を見て選びます。
- **SNMP の結果を保存するとき、ダイアログを開いている間に次の取得が
  終わると、内容と対象がずれて記録されていた。** 保存を選んだ時点の
  内容を書きます。
- **`custom_mibs.json` を書き換えても、MIB の変換結果が古いままだった。**
  内容の変化と解析器の更新を見て作り直します。
- **MIB の定義の読み取りが、次の定義まで飲み込んで名前と OID を
  取り違えることがあった。** 定義の切れ目を正しく見ます。あわせて、
  名前や親名にハイフンを含む定義（`mib-2` など標準 MIB のほとんどの
  親）を読めるようになりました。これまでは `mib-2` が名前「2」に
  なり、その配下が丸ごと名前に変換できませんでした。
- **同じ名前の定義が複数の MIB にあると、片方の OID しか残らなかった。**
  モジュールごとに区別します。IMPORTS は読まないという制限は残ります。
- **実行ファイルを別のフォルダから起動すると、`mibs` を読めず、
  起動した場所に `mib_cache.json` を作っていた。** 実行ファイルの
  置き場所を基準にします。

- **名前を付けていないグループが複数あると、空のグループを削除した
  つもりで別のグループの機器まで消えた。** どれも同じ「(名前なし)」
  として扱っていたためです。
- **自動検出した COM ポートと登録済みの機器が同じ名前で並びうるため、
  別の機器のグループ自動コマンドがコンソールへ流れた。** 自動検出の
  項目には印を付け、グループの自動コマンドの対象から外します。
- **機器の名前を変えても、接続とタブは古い名前のまま残っていた。**
  その古い名前を別の機器に付け直すと、新しい機器へ送ったつもりの自動
  コマンドが前の接続へ飛びました。開いているタブや接続が持っている名前は、
  別の機器に使えません。
- **Enter による再接続に失敗すると、以後 Enter を押しても再接続できなく
  なった。** 画面には「Enter キーを押すと再接続します」と残ったままでした。
  待機は接続に成功した時点で解きます。
- **自動検出した COM ポートのボーレートを変えても、開いているタブの
  再接続には反映されなかった。** Enter での再接続だけ古い値のままでした。
- **グループの追加・削除で保存に失敗しても、画面と実行中の設定が
  食い違ったままだった。** 「失敗した」と案内した変更が、あとの保存で
  永続化されることがありました。
- **共通マクロの追加・更新・削除は、保存に失敗してもメモリ上の内容が
  戻らなかった。** 取り消したはずのコマンドが実行される状態でした。
- **グループの `devices` が壊れている設定ファイルで、機器の追加が
  例外になった。** 読み込みの時点で整えます。
- **SSH の既知ホスト鍵（known_hosts）の保存で、保存済みの鍵を失うことが
  あった。** 書き込みが途中で失敗すると元のファイルが壊れ、同時に 2 つ
  保存すると片方が消えていました。一時ファイルへ書いてから差し替え、
  保存の直前に他が書いた鍵を取り込みます。
- **古い保存先からの known_hosts の引き継ぎに失敗すると、黙って
  「未知のホスト」として扱っていた。** 失敗を知らせ、やり直せるように
  しました。
- **タブを閉じたあとに成立した SSH セッションが回収されなかった。**
  接続が確立する前に閉じた場合です。後から成立した接続も閉じます。
- **シリアルポートを閉じるとき、送信スレッドがまだ書いている最中でも
  閉じていた。** 待ち時間を過ぎた場合です。後始末を送信側へ渡します。
- **Telnet の交渉応答だけ、送信の結果を確かめていなかった。** 送りきれて
  いなくても気づかず、相手が応答を待ち続けることがありました。
- **SFTP の送信で使う一時名が固定だったため、前回の失敗で機器側に残った
  「唯一の完全な写し」を次の送信が上書きしていた。** 送信ごとに別の名前を
  使います。
- **SFTP で既存ファイルを置き換えると、元のアクセス権が失われていた。**
  元の権限を引き継ぎます。
- **上書きの確認を経ていない送信が、確認から置き換えまでの間に現れた
  同名のファイルを黙って潰していた。** 置き換える直前にもう一度確かめます。
- **ルート直下へ送ると、一時ファイルだけがホームディレクトリに作られて
  いた。** ルート直下の一時名にします。
- **SFTP の応答が期限切れになっても、接続中のまま残る経路があった。**
  以後の操作が毎回期限切れまで待たされました。使えない接続は畳みます。
- **ディレクトリを指すシンボリックリンクが、パネルから開けなかった。**
  リンク先の種別で判断します。
- **順番待ちの間に切断されたダウンロードが、空の一時ファイルを残して
  いた。**
- **リモートの「親へ」が、名前にバックスラッシュを含むディレクトリで
  別の場所へ移動していた。** リモートのパスは常に `/` 区切りとして
  扱います。
- **ポート確認で、`netstat` の実行に失敗しても「使用されていません」と
  表示していた。** また、IPv6 だけで待ち受けているポートを見落として
  いました。
- **SNMP で、前の操作の結果が次の操作のホストの結果として記録されて
  いた。** 結果が画面へ届く前に次の要求を受け付けていたためです。
- **SNMP のエクスポートが、書き込みに失敗すると上書き先の既存ファイルを
  失っていた。** 6 つの保存経路すべてです。一時ファイルへ書いてから
  置き換えます。
- **Trap の受信時刻ではなく、画面が処理した時刻を表示・保存していた。**
- **保存先の拡張子が大文字（`.CSV` / `.JSON`）だと、形式を取り違えた
  まま「成功」と表示していた。**
- **標準 MIB の配下にある名前が一つも解決できなかった。** 起点
  （iso / internet / mgmt / mib-2）が初期の辞書に無かったためです。
- **`OBJECT-IDENTITY` で定義された枝が抽出されず、その配下がまとめて
  解決できなかった。**
- **壊れた `custom_mibs.json` を読み込むと、その値が読み込みエラーの
  あとも残り、その OID の Trap が表に出なかった。**
- **`mibs/` フォルダの拡張子判定が大文字と小文字を区別していた。**
  `CASE.MIB` のようなファイルが解析されませんでした。

- **接続を捨てたあとに接続処理が始まると、その印が消えて接続が成立して
  いた。** タブを閉じた直後に起きます。SSH では誰も持っていないセッションが
  機器側に残り、シリアルでは COM ポートを掴んだままになって、アプリを
  再起動するまで同じ機器へ繋げませんでした。
- **順番待ちの間に切断された送信と一覧取得が、成功も失敗も知らせずに
  消えていた。** 利用者からは送れたのかどうか分かりませんでした。
- **ディレクトリを指すリンクを、パネルから削除できなかった。** リンクを
  ディレクトリとして扱うようにした副作用です。移動はリンク先、削除は
  リンク自身、と分けました。
- **SFTP の置き換えで、最後のやり直しの応答が期限切れになったときだけ、
  「失敗しました」と断定していた。** 実際は置き換わったかどうか
  分かりません。理由が空のまま終わることもありました。
- **機器名として、一覧に出ている自動検出の COM ポート名を登録できた。**
  あとでどちらかに繋ぐと、もう一方は実態と食い違う案内だけが出ました。
- **グループ名の重複で登録を断ったときに、機器一覧が作り直されていた。**
  設定は何も変わっていないのに、畳んでいたグループが開き直り、選択も
  外れていました。
- **ポート確認で `netstat` や `tasklist` を起動できない環境では、結果が
  一切表示されなかった。** 「取得に失敗しました」という案内も出ません
  でした。
- **標準 OID ツリーの起点がまだ足りず、`experimental` / `security` /
  `snmpModules` の配下が解決できなかった。** 標準の Trap 名
  （`coldStart` など）はここにぶら下がります。
- **SNMP のエクスポート先を他のプログラムが開いていると、原因の分からない
  保存失敗になっていた。** どのファイルが使用中かを伝えます。

### 修正（サーバ機能・更新とビルド）
- **サーバの動作中でも公開フォルダを選び直せてしまった。** 動作中は
  選べません。
- **TFTP が netascii 指定を無視して、そのまま転送していた。** 改行を
  変換します。
- **TFTP の最後の応答が届かなかった機器が、再送しても何も返って
  こなかった。** 転送の終わりに少し待ち、再送に応えます。
- **TFTP のタイムアウト指定を受け取っても使っていなかった。** 1〜255 秒
  の範囲で待ち時間と再送間隔に反映します。
- **SFTP サーバの停止中に届いた接続を、そのまま受け付けていた。**
  停止中は閉じます。
- **SFTP サーバの公開フォルダにドライブ直下（`D:\`）を指定できなかった。**
  指定できます。
- **Syslog の選んだ行を保存するとき、ダイアログを開いている間に新しい
  メッセージが届くと、別の行が保存されていた。** 選んだ時点の内容を
  保存します。
- **Syslog の書き出しに送信元アドレスやプロトコルが入っていなかった。**
  別々の機器の行が、保存したあとで見分けられませんでした。テキストにも
  JSON にも「送信元 (プロトコル/ポート)」を入れます。受信した生の
  メッセージは JSON にのみ記録します。右クリックの「コピー」も
  同じ書式になりました。
- **UTF-8 として読めないメッセージが、代替の文字コードへ回らずに
  文字が落ちていた。** 読めない場合に latin-1 へ回します。
- **TCP の Syslog が、長さを前置きする形式（RFC 6587）を受け取れな
  かった。** 受け取れます。
- **RFC 5424 の構造化データを空白で切っていたため、引用符や括弧を
  含む値が壊れていた。** 括弧・引用符・エスケープを見て切ります。
- **TCP の Syslog に接続数の上限が無かった。** 64 までにし、超えた分は
  断ります。
- **ファイアウォールの規則を名前の一致だけで「登録済み」と判断して
  いた。** 無効化された規則やブロック規則があると、通信できないのに
  登録済みと表示されていました。状態と動作を見て判断し、直せるものは
  直します。許可を要求したあと反映を確認できない場合は、成功にせず
  失敗として知らせます。日本語・英語以外の表示言語では `netsh` の
  出力を読めないため、これまでどおり名前の一致だけで判断します。
  各パネルの「ファイアウォールで許可」の結果に、理由も出るように
  なりました。
- **更新後に再起動した実行ファイルが、Python の DLL を読めずに落ちる
  ことがあった。** 再起動時に環境変数を初期化します。
- **更新のダウンロード中にダイアログを閉じると、約 10 秒固まったり
  異常終了したりしていた。** すぐ閉じ、ダウンロードは裏で止めます。
- **更新スクリプトが、利用者自身の `backup_` で始まるフォルダまで
  消していた。** 中に 7 日以上前のファイルが 1 つでもあれば、当日の
  ファイルごとフォルダを削除していました。更新スクリプト自身が付ける
  名前（`backup_netbelt_`）に限り、フォルダ自身の日付で判断します。
- **更新の途中で実行ファイルの差し替えが中断すると、壊れた実行ファイル
  が残っていた。** 別名で展開してから入れ替えます。失敗した場合は
  元の実行ファイルが残ります。
- **`build.bat` を別のフォルダから実行すると、そのフォルダの `dist`
  `build` を消していた。** スクリプトのある場所を基準にします。
- **起動時に「更新を適用しますか」に「はい」と答えても、アプリが
  終了せず更新に失敗していた。** 終了するようにしました。
- **ダウンロードしたファイルの名前がバージョンによらず同じで、
  別のバージョンの内容を適用することがあった。** バージョンごとの
  名前にし、適用前に中身を照合します。同時に 2 つ受信すると同じ
  一時ファイルを奪い合って両方失敗し、検証済みのファイルまで
  消えていた問題も直しました。未適用のものが複数あるときは、
  いちばん新しいものを勧めます。
- **ソースコードから実行しているときも、実行ファイル向けの更新を
  適用しようとしていた。** その場合は `git pull` を案内します。

- **Syslog の保存・エクスポートが、書き込みに失敗すると上書き先の
  既存ログを失っていた。** 一時ファイルへ書いてから置き換えます。
- **Syslog の本文に改行が入っていると、テキスト保存やコピーで別の機器の
  独立した記録に見える行を作れた。** 本文の改行はエスケープし、1 件が
  1 行になるようにします。
- **日時の無い Syslog は、本文の先頭 4 語が日時とホスト名として食われて
  いた。** 画面・検索・保存から本文が落ちていました。日時の形を確かめて
  から取り込みます。
- **Syslog の保持件数の上限は、画面へ届く前に溜まっている分を制限して
  いなかった。** 短時間に大量に届くとキューが際限なく伸び、画面が長時間
  固まりました。待ち行列にも上限を設けます。
- **TCP で受けた Syslog の受信原文が、本文末尾の空白や改行を削ったものに
  なっていた。**
- **TFTP で、タイムアウト以外の失敗のあと、同じ名前の転送の開始通知と
  次の失敗通知が出なくなった。** 内部の対応表から外れないためです。
- **TFTP の停止ボタンが、大きなファイルの netascii サイズ計算の間、
  画面ごと長時間固まった。** 停止の要求で計算を打ち切ります。
- **SFTP サーバの未認証の接続数に上限が無かった。** 接続のたびにスレッドと
  セッションが際限なく積み上がりました。同時接続数（既定 32）と、
  バナー・認証の待ち時間に上限を設けます。
- **SFTP サーバの LSTAT が、リンク自身ではなくリンク先の情報を返して
  いた。**
- **FTP / TFTP / SFTP パネルのアクティビティログと転送履歴に保持上限が
  無く、認証を通らない相手からでも際限なく増やせた。** どちらも 1000 件
  までにします。
- **FTP / SFTP サーバのパスワードから、前後の空白が黙って取り除かれて
  いた。** 画面の表示と実際の資格情報が食い違っていました。
- **同じポートに有効な許可規則と拒否規則が両方あると、ファイアウォールの
  修復を「問題なし」として飛ばしていた。** 実際には通信できない状態でした。
- **改行を含む GitHub トークンが、例外のメッセージ経由でログと画面に
  そのまま出ていた。**
- **更新の一時フォルダを作れないだけで、アプリが起動できなくなって
  いた。**
- **更新の中止が、ダウンロードの読み取りが終わるまで画面を止めていた。**
- **更新を適用する直前に、ZIP の中身と版をもう一度確かめるようにした。**
  確認ダイアログを閉じるまでの間に、ファイルが消えたり別の版へ差し替わったり
  しても、そのまま展開していました。
- **実行ファイルを `NetBelt.exe` 以外の名前に変えて使っていると、更新が
  当たらないのに「成功」と表示し、身に覚えのない `NetBelt.exe` だけが
  増えていた。** 名前が違う場合は、何も展開せずに中止して理由を示します。
- **ネットワーク上の共有フォルダ（UNC パス）から `build.bat` を呼ぶと、
  呼び出し元の `dist` / `build` を削除することがあった。** 自分の
  フォルダへ移れなかった時点で止めます。
- **`build.bat` が、実行ファイルの有無だけを見て「ビルド成功」と表示して
  いた。** 古い実行ファイルが残っているだけでも成功に見えました。

- **Syslog の本文が「数字＋空白」で始まると、RFC 5424 の書式と取り違えて
  本文の先頭が食われていた。** 日時の形を確かめてから振り分けます。
- **Syslog の保存先の拡張子が大文字（`.JSON`）だと、形式を取り違えたまま
  「成功」と表示していた。**
- **Syslog の保存先に、端末が記録中のログを選べてしまった。**
  どの機器が使っているかを示して断ります。
- **SFTP サーバの一覧に、リンク先を失ったリンクが出てこなかった。**
  ディスク上には在って削除もできるのに、一覧からは消えていました。
  一覧はリンク自身の情報を返します。
- **`build.bat` で `updater.bat` のコピーに失敗したときだけ、画面には
  「ビルド失敗」と出るのに終了コードは 0 だった。**
- **`build.bat` の日本語が文字化けし、ときどき行の一部がコマンドとして
  実行されていた。** コードページを決めてから読み直すようにしました。

- **同じ場所へ同時に更新を当てると、成功と表示した側が相手の
  実行ファイルを据えていた。** 差し替え用の一時名がインストール先で
  共通だったためです。もう一方には「旧版のままです」と、事実と違う案内が
  出ていました。一時名を実行ごとに分けます。
- **更新ファイルを確かめてから展開するまでの数秒の間に、別の内容へ
  差し替えられると、そのまま適用されていた。** 確かめた実体そのものを
  渡すようにしました。
- **起動できない実行ファイルに差し替わっても「更新が完了しました」と
  表示し、更新ファイルを消していた。** 起動を確かめ、できなければ
  更新ファイルを残して理由を示します。
- **更新ファイルの控え（ハッシュと版）を保存できなくても「ダウンロード
  完了」と表示していた。** そのままでは適用時に必ず拒否されます。
  控えまで揃ってから完了とし、揃わなければ書きかけを片付けます。
- **ダウンロードを中止しても、ハッシュを取りに行っている間だったときは
  中止されず、次の起動で「未適用の更新があります」と出ていた。**

### 8 月 28 日までに入っていた変更

全サブシステムを対象に不具合を洗い出し、見つかったものを直しました。
利用者からの報告ではなく、テストが見ていない範囲を調べた結果です。

**設定や接続先の作り直しは不要です。** これまでの `config.json` を
そのまま使えます。

**ファイアウォールの扱いが変わりました。** Syslog 受信・SFTP サーバ・
SNMP Trap 受信が、起動時に Windows ファイアウォールの受信許可を
自動で作るのをやめました（TFTP と FTP は以前からこの方式です）。

**これまで使っていたポートは、そのまま受信できます。** 自動作成された
ルールは消さずに残るためです。影響があるのは、更新後に**はじめて使う
ポート**へ変えたときだけで、その場合は各パネルに増えた
「ファイアウォールで許可（管理者）」を一度押してください。

なお、これまでに作られたルールは `NetBelt - Syslog (UDP/514)` のように
ポート番号ごとに残っています。使っていないポートのものは、Windows の
「セキュリティが強化された Windows Defender ファイアウォール」から
削除できます。

#### 追加
- **匿名 FTP の「ログインの許可」と「書き込みの許可」を分けました。**
  これまで「匿名を許可」にチェックを入れると、資格情報なしで上書き・
  削除・改名・フォルダ作成まで許していました。書き込みは別のチェック
  ボックスになり、許可した場合も機器がファイルを置くのに要る範囲だけに
  絞ります（削除・改名・フォルダ作成は含みません）。
- **Syslog・SFTP サーバ・SNMP の各パネルに「ファイアウォールで許可
  （管理者）」を足しました。** 受信できないときに押してください。
  押したときだけ管理者への昇格を求めます。これで 5 つのパネルすべてで
  同じ手順になります。
- **受信した SNMP Trap の保持件数に上限を設けました。** 既定は 1000 件で、
  `config.json` の `settings.snmp.max_traps` で変えられます。Syslog の
  メッセージ上限と同じ考え方です。

#### 修正
- **SFTP の一覧を開いただけでアプリが落ちることがあった。** 更新日時や
  サイズを返さないサーバがあり、その値をそのまま扱っていたためです。
  一覧の失敗では済まず、ターミナルのタブや起動中のサーバごと消えていました。
  値が無い場合は「不明」と表示します。
- **SFTP の操作が、別の機器へ飛ぶことがあった。** ファイル選択などの
  ダイアログを開いている間にタブを切り替えると、送り先が切り替えた先の
  機器になっていました。落ちない代わりに、送るつもりのないルータへ
  ファイルが置かれ、削除も同じ経路で起きます。操作を始めた機器と
  同じでなければ、取りやめるようにしました。同じ機器の中でも、
  ダイアログを開いている間に一覧の取得が終わると送り先の
  ディレクトリが変わっていました。操作を始めた時点のディレクトリ
  に対して実行します。画面に出ている一覧と操作の対象が一瞬でも
  食い違わないよう、一覧の反映と現在地の更新を同時に行います。
- **設定の保存が途中で止まると、機器の登録がすべて消えていた。**
  保存先のファイルを先に空にしてから書いていたためです。強制終了・
  シャットダウン・ディスク満杯のいずれでも起き、次回起動時の
  バックアップにも壊れた中身しか残りませんでした。書き上げてから
  差し替える方式に変え、失敗しても前の設定が残ります。
- **日本語入力で確定した文字が、機器へ送られていなかった。** 画面には
  出るのに機器は受け取っていない状態になり、さらに過去の出力を範囲選択
  したまま確定すると、その出力が確定文字で置き換わって元に戻りません
  でした。全ログ保存にもそのまま残っていました。
- **出力がスクロールバックに残らないことがあった。** 機器側のアプリが
  画面の一部だけを使う指定を出したあと、元へ戻す指定が受け付けられずに
  残り続けるためです。以後の出力が記録に入らなくなっていました。
- **ウィンドウを狭めた直後の出力に、覚えのない文字が混ざることがあった。**
  狭める前にその行にあった文字が、折り返し位置へ差し込まれていました。
- **絵文字などが 1 つ画面に出ると、以後の表示がずれ続けた。** 行が繋がる、
  文字が欠ける、といった形で崩れ、直りませんでした。
- **設定を貼り付けている間、ウィンドウが反応しなくなった。** 1 文字ずつ
  送っていたためです。まとめて送るようにし、送っている間も画面が
  動きます。
- **コンソール接続が、一度エラーで切れると二度と繋がらなくなった。**
  切断を検知したあともポートを掴んだままだったためで、アプリを再起動
  するまで「アクセスが拒否されました」になっていました。タブを閉じても
  直りませんでした。
- **Telnet の機器を「編集」で開くだけで、ポート番号が 23 に戻っていた。**
  何も触らずに OK を押しただけで書き換わり、そのまま保存されます。
  ターミナルサーバや CML のように 2001 番以降を使う機器へ、以後
  繋がらなくなっていました。SSH の機器では起きないため気づきにくい
  不具合でした。
- **Telnet でネゴシエーションが分割して届くと、表示が崩れることがあった。**
  制御用のバイトが画面へ漏れ、機器へ応答も返さないため、待つ実装の
  機器ではプロンプトが出ないままになっていました。ネゴシエーションの
  本文に特定のバイト並びがあると終端を見誤って本文が画面へ漏れる、
  大きすぎるものを読み飛ばしている最中に終端が分割して届くと以後の
  出力を全部捨て続ける、という 2 つの取りこぼしも直しました。
- **認証は通るのにシェルが開かない機器で、接続が固まったままになった。**
  待ち時間の上限が無く、失敗の表示も再接続の案内も出ないため、
  ボタンを押しても何も起きないように見えていました。
- **SSH の接続に失敗するたび、機器側のセッションが残っていた。**
  パスワードを続けて打ち間違えると、機器の vty を一時的に埋めます。
- **ログ記録の停止が、別の機器のタブに効いていた。** 2 台を同時に記録
  しているとき、片方のダイアログで停止を押すと、止まるのは表示中の
  もう一方でした。押した側は記録が続いたままダイアログだけ消えるため、
  止めたつもりの内容がディスクに残り続けていました。記録中のタブを
  閉じたときも、ファイルが開いたままでした。
- **エラーで落ちたとき、ウィンドウが消えるだけで何も残らなかった。**
  記録用のログにも 1 バイトも残らないため、何が起きたのかを後から
  調べる手がかりがありませんでした。原因を記録したうえで内容と記録先を
  表示し、操作ひとつの失敗でアプリごと終わらせないようにしました。
  起動できないときの理由も、同じログに残ります。
- **ツールを別ウィンドウで開いたまま閉じると、アプリが終了しなかった。**
  メインウィンドウは消えるのにプロセスが残っていました。
- **TFTP サーバが、要求の数だけ制限なく処理を起こしていた。** 待ち受けが
  止まっても「起動中」と表示され続け、以後どの転送も成立しませんでした。
  同時に扱う転送数に上限を設け、超えた要求はその場で断ります。
- **FTP サーバや SFTP サーバを停止した直後にパネルやアプリを閉じると、
  落ちることがあった。** 停止の処理が待受スレッドの終了を待たずに
  戻っていたためで、まだ動いているスレッドからの通知が片付け済みの
  画面へ届いていました。終了を待ってから戻ります。SFTP サーバでは
  「開始」の直後に「停止」を押すと止まらないことがあったのと、接続した
  まま何も送らない相手がいると停止に相手の数だけ秒単位で固まっていた
  のも直しました。
- **起動した直後にアプリを閉じると、落ちることがあった。** SNMP の
  MIB を裏で読み込んでいる最中にウィンドウが片付けられるためで、
  読み込みが終わるのを待ってから閉じます。
- **Syslog の TCP 受信で、改行を送らない相手がいるとメモリを使い続けた。**
  1 行の長さに上限を設け、超えた接続は切って記録に残します。行の終端が
  CRLF でも LF でも、同じ長さまで受け付けます。
- **`mibs/` に置いた MIB がほとんど名前に変換されなかった。** ファイルを
  またいだ参照が解決できず、現代の MIB が使うモジュール定義も読んで
  いなかったためです。説明文に「RFC 1234」のような参照や URL が
  書かれた定義も、まとめて読み飛ばしていました。Trap が運ぶ OID が
  数字のままになっていました。コメントアウトされた古い定義や、説明文の
  中に書かれた例を本物の定義として読んでしまい、別の名前が付いたり
  本物が消えたりする取り違えも直しました。
- **エクスポートした CSV が、表計算ソフトで開いた瞬間に数式として
  実行されることがあった。** 機器側で自由に書ける値がそのまま書き出されて
  いたためです。障害チケットや報告書へ回した先の端末で起きます。
- **2GiB を超える転送で、進捗の表示が当てにならなかった。** 2〜4GiB では
  バーが動かず、4GiB を超えると 5 分の 1 ほど進んだ時点で「100%」に
  達していました。完了したと誤解して転送中にアプリを閉じかねない
  状態でした。
- **転送中に別のタブへ切り替えると、前の機器の進捗が残り続けた。**
  接続先の表示は切り替わるのに、その下のバーは前の機器の途中経過を
  出したままでした。
- **更新の適用が当たらないことがあった。** 更新の途中で更新用スクリプトが
  自分自身を置き換えてしまい、そこから先が壊れていました。アプリだけ
  終了して更新が適用されない、という形になります。
- **インストール先のパスによっては、更新がまったく当たらなかった。**
  パスにコンマや等号が含まれる場合で、アプリが終了するだけで何も
  起きませんでした。`^` `%` `&` `!` を含む場合は引用符では救えないため、
  理由を表示して中止します（`!` は一時フォルダ TEMP のパスに含まれる
  場合も同じで、以前は「TEMP の空きが無い」という見当違いの案内に
  なっていました）。
- **ダウンロードが途中で切れると、書きかけのファイルが残り続けた。**
  掃除の対象にもならず、再試行しない限り消えませんでした。検証済みの
  ZIP の傍らに残るファイルも、あわせて片付けます。
- **GPL の対応ソースから作った NetBelt は、自動更新がまったく効かなかった。**
  リリースに添付されるソースアーカイブでは、更新用スクリプトの改行
  コードが Windows のものにならないためです。`core.autocrlf` を無効に
  してクローンした場合も同じでした。

#### 変更
- Syslog 受信・SFTP サーバ・SNMP Trap 受信は、起動時に Windows
  ファイアウォールの受信許可を作らなくなりました。TFTP と FTP に
  揃えたもので、「受信開始」を押しただけで管理者への昇格を求めない
  ためです。これまでは待ち受けるたびにポート番号入りのルールが恒久
  登録され、停止しても消えずに溜まっていました。既に作られたルールは
  残るので、これまでのポートはそのまま受信できます。新しいポートで
  通らない場合は、各パネルのボタンで許可を足せます。
- SNMP の WALK が途中で応答を失っても、そこまでに取れた行を表示する
  ようにしました。これまではエラーだけが出て、取得済みの行はすべて
  捨てられていました。全部ではないことが分かるよう、件数と中断の理由を
  画面に添え、txt / csv / json へ書き出したファイルにも同じ注意を
  残します（保存した結果が完走したものと区別できるように）。1 行も
  取れていない場合はこれまでどおりエラーです。
- 匿名 FTP の書き込みは、既定では許可しません。これまで「匿名を許可」で
  運用していた設定は、書き込みを許可した状態のまま読み込みます
  （機器からのアップロードが更新後に止まらないようにするためです）。
- SNMP Trap は既定で最新 1000 件までを保持します。それより古いものは
  順に捨てられます。これまでは上限が無く、受信を続けるほどメモリを
  使い続けていました。

## [1.2.0] - 2026-08-27

ターミナルの描画を作り直しました。これまでは受信した文字を書き足していく
だけで、行と桁からなる「画面」という概念を持っていませんでした。そのため
`nano` や `vi` が「23 行目の 1 桁目へ」と指示しても行き先が無く、画面ぶんの
描画がすべて同じ場所に重なっていました。

**設定や接続先の作り直しは不要です。** これまでの `config.json` をそのまま
使えます。

### 追加
- **`nano` や `vi` の画面が崩れなくなった。** これまでは「対応していません」と
  案内を出すのが精一杯でした。タイトルバー・編集領域・下部のショートカット
  2 段が、実際の画面として組み上がります。`less` や `top` のような画面を
  作るコマンドも同じ仕組みで扱いますが、すべてのコマンドを試したわけでは
  ないため、崩れるものが残っている可能性はあります。
- **全画面のコマンドの画面は、記録にも残ります。** `nano` や `vi` を閉じても
  開く前の画面は戻らず、編集中の画面がそのままスクロールバックと
  セッションログに残ります。NetBelt は端末種別を `vt100` として接続するため、
  これらのコマンドが画面を退避する仕組み（代替画面）を使わないためです。
  編集内容を記録に残したくない場合は、ログ記録を止めてください
  （スクロールバックには残りますが、画面上だけでファイルには残りません）。
- **色と装飾を表示するようになった。** 文字色・背景色・太字・下線・反転に
  対応しました。基本 16 色に加えて 256 色パレットと RGB 直接指定も扱えます。
  反転表示は、設定した文字色と背景色を入れ替えて表示します。
- **ウィンドウの大きさを機器へ伝えるようになった。** これまでは 80×24 固定で、
  ウィンドウを広げても `show` の出力が 80 桁で折り返していました。接続時と
  リサイズ時に実際の行数・桁数を伝えます（RFC 4254 §6.2 / §6.7）。
  この通知は SSH のみです。Telnet とシリアルでは通知しませんが、
  画面の描画・色・折り返しはこれまでどおり同じように働きます。
- **アプリケーションが指定する入力方式に従うようになった。** `nano` が
  要求するカーソルキーの形式（DECCKM）と、bash のブラケットペーストに
  対応しました。貼り付けが 1 行ずつ実行されず、まとめて編集できます。
- **ステータスバーに端末の大きさを表示するようにした。** いま表示中の
  ターミナルの桁数と行数が右端に出ます（SSH ではこの値を機器へ伝えて
  います。Telnet とシリアルでは表示だけです）。
- **ターミナルのタブをドラッグで並べ替えられるようにした。**
- **接続先リストを隠せるようにした。** ツールエリアと同じく、仕切りを
  ドラッグする・右クリックする・表示メニューから切り替える、の 3 通りです。
- **更新の通知に、何が変わったかを出すようにした。** これまでは使い方の
  案内しか出ておらず、更新して何が変わるのか分かりませんでした。
  何世代か飛ばしている場合は、その間のぶんもまとめて表示します
  （多すぎると読まれないため、5 世代までとし、残りは件数だけ添えます）。

### 修正
- **`nano` や `vi` を開くと画面が 1 行に潰れていた。** 上の「追加」の
  とおり、画面として組み上がるようになりました。
- **ウィンドウを広げても、出力が 80 桁で折り返したままだった。**
  端末の大きさを 80×24 に固定して機器へ伝えていたためです。

### 変更
- 受信の解釈を、正規表現による逐次処理から仕様どおりの状態機械へ置き換え
  ました。受信がどこで途切れても解釈が変わりません。実機から採取した
  データを分割して検証しています（Linux 側の 3 種は全ての切れ目、
  機器から採った 7058 バイトは 7 バイトおき）。
- 画面の大きさが変わったとき、すでに書かれた行は組み直しません（実端末と
  同じ挙動です）。狭い状態で出力された行は、その折り返しのまま残ります。
  ただし表示領域から流れて記録に入った行は 1 本の行として保存されるため、
  ウィンドウの幅に合わせて折り返し直されます。
- 全画面アプリを検知して案内を出す仕組みを廃止しました。実際に表示できる
  ようになったため不要になりました。
- 機器から端末への問い合わせ（カーソル位置・装置種別）に、自動で答える
  ようになりました。答えないと待ち続けるアプリケーションがあるためです。
  送るのは問い合わせへの応答だけで、こちらから何かを送ることはありません。
- 端末の高さを機器へ伝えるようになったため、機器側のページャ（`--More--`）
  の区切りがウィンドウの高さで変わります。ウィンドウが高いほど一度に
  多く表示されます。
- 全角文字は 1 桁として数えます。日本語を含む行の折り返し位置が、
  実際の見た目とずれることがあります。機器の出力は英数字が中心のため、
  影響は Linux 側での日本語編集に限られます。

### 開発メモ
作り直しは仕様から行っています。既存のターミナル実装のソースコードは
読んでいません（読んで書き直すと、構造や命名が似たときに派生物とみなされる
余地が残るため）。参照したのは Paul Williams の DEC 互換パーサ状態遷移図、
ECMA-48（= ISO/IEC 6429）、ISO/IEC 2022、RFC 4254、および標準に無い部分に
ついては XTerm Control Sequences です。

## [1.1.1] - 2026-08-26

公開後に見つかった不具合の修正です。

### 修正
- **SSH の公開鍵認証がまったく使えなかった。** 対応する鍵の種類を並べる箇所に、
  paramiko 4 で削除された DSA を残していたため、正しい鍵でも
  「秘密鍵の読み込みエラー」で接続できなかった。パスワード認証は別経路のため
  動いており、気づかれないまま 1.1.0 に入っていた。
  あわせて、鍵を指定したときに `~/.ssh` の別の鍵で認証が通ってしまう問題と、
  パスフレーズ付きの鍵で原因が分からないメッセージが出る問題も直した。
- **SFTP クライアントで、複数ファイルを同時に転送すると壊れていた。**
  まとめてドロップすると転送が互いを壊し、相手側に中途半端なファイルや
  0 バイトのファイルが残っていた。エラーも出なかった。
- **SFTP クライアントが、表示中のターミナルと違う機器を指したままになっていた。**
  2 台以上に接続してタブを切り替えると、前に繋いだ機器へファイルを送っていた。
  どの機器かパネルに出ていなかったため、気づく手段もなかった。
  接続先の機器名とアドレスをパネル上部に表示するようにした。
- **TFTP サーバを停止しても、進行中の転送が止まっていなかった。**
  停止後にファイルが作られ、アプリ終了時には受領済みと応答したデータが
  保存されないまま失われていた。
- **TFTP のエラー表示。** どの機器から来た要求か分からず、機器都合の失敗でも
  ダイアログが出続け、1 件の失敗で進行中の転送すべてがエラー表示になっていた。
  送信元を添えてログに出し、該当する転送だけを対象にするようにした。
  サーバ自体の障害（ポート使用中など）は今までどおりダイアログで知らせる。
- **FTP サーバのパスワードが設定ファイルに平文で保存されていた。**
  機器のパスワードと同じく Windows DPAPI で暗号化するようにした。
  既存の設定は次回保存時に暗号化される。
- **更新の確認。** ダウンロード済みの更新ファイルにバージョンが記録されておらず、
  古いものが残っていると新しい版から古い版への適用を促していた。
  チェックサムの控えを取り違える経路も直した。
- Syslog サーバの停止処理が、受信中のソケットを別のスレッドから閉じていた。
  Windows では不正な操作で、まれにアプリが落ちる原因になる。
- **自動更新が、成功しているのに「起動に失敗しました」と報告していた。**
  `start` は成功しても直前の終了状態を引き継ぐため、その値で成否を
  判定していたのが原因。あわせて、更新スクリプトの日本語がコンソールで
  化けて行の断片がコマンドとして実行される問題と、コピー後に実行ファイルが
  置かれたかを確認していなかった問題も直した。
  **この修正が効くのは 1.1.1 以降からの更新です。** 更新時に動くのは
  インストール済みの版に同梱されたスクリプトのため、1.0.0 / 1.1.0 から
  1.1.1 へ上げるときは、これまでどおり文字化けと誤った失敗表示が出ます。
  ファイルの入れ替え自体は成功しているので、手でアプリを起動し直せば
  更新は完了しています。
- **括弧を含むフォルダに置いていると、更新が途中で止まっていた。**
  `Program Files (x86)` や、同じファイルを 2 回ダウンロードしたときの
  `... (1)` のようなフォルダ名だと、「was unexpected at this time」と出て
  更新が当たらないまま終わっていた。1.1.0 でも同じだった。
  同じ調査で、`&` `,` `=` `'` を含むフォルダ名でも更新が当たらないことが
  分かったので、あわせて直した（いずれも 1.1.0 から壊れていた）。
  なお `!` を含むフォルダ名では、まだ更新できません。遅延展開が
  この文字を消してしまうためで、括弧への対策と両立しません。
  黙って失敗しないよう、理由と回避策を出して中止するようにしました。
- **ユーザー名を空のまま SSH の機器を登録できていた。**
  SSH はユーザー名が無いと必ず認証に失敗するため、その機器は保存した
  時点で二度と繋がらない。登録のときに止めるようにした
  （telnet と console はこれまでどおり空でよい）。
- **認証に失敗したとき、鍵で接続していてもパスワードのせいにしていた。**
  「ユーザー名またはパスワードが間違っています」の一点張りだったため、
  渡してもいないパスワードを疑うことになっていた。ユーザー名が空なら
  そう伝え、鍵のときは authorized_keys とユーザー名の確認を促す。
- **機器を編集しても、開いているタブの再接続に反映されていなかった。**
  鍵・パスワード・ユーザー名・ホストのどれを変えても、Enter による
  再接続は編集前の情報で繋いでいた。パスワードを変えて締め出した
  つもりでも、そのタブからは入れてしまう。機器を削除したときも同じで、
  消したはずの機器へ繋がっていた。
- **暗い配色のとき、SFTP クライアントの接続先が読めなかった。**
  帯の背景色だけを決め打ちしていたため、明るい文字色と同系色になって
  消えていた。どの機器へ送るのかを確かめるための表示なので、
  読めなければ意味がない。配色に追従させた。
  あわせて、FTP / SFTP サーバ / TFTP の説明ラベルと、ログ保存・記録
  ダイアログの補足文も同じ仕組みに寄せた。前者は暗い配色でも読めるが
  白いチップが浮いており、後者は暗い配色で文字が沈んでいた。
- **受信が途中で切れると、エスケープシーケンスの残りが文字として画面に
  出ていた。** 機器からのデータは届いた分ずつ処理するため、1 つの指示が
  2 回に分かれることがある。これまでは切れた指示を捨てて先へ進んでおり、
  `[?2004l` のような文字列が紛れ込んでいた。次のデータと繋げてから
  解釈するようにした。実機の採取データを全ての位置で分割して確認した
  （527 バイト中 161 箇所で表示が変わっていたものが、0 箇所になった）。
- 画面消去の指示（`ESC[2J`）で、表示そのものを削除していた。届いた
  場合にセッションの記録ごと失うため、端末と同じように上へ送り出して
  見えなくするだけに変えた。なお端末種別を vt100 と名乗っているため、
  Linux の `clear` はこの指示を送らない（`ESC[H` `ESC[J` が届く）。
  実際に踏む経路は確認できていない。

### 変更
- ターミナルへドラッグ＆ドロップしたテキストを、貼り付けと同じように
  機器へ送るようにした。以前は画面にだけ入り、機器は受け取っていなかった。
- `Ctrl` + マウスホイールでのフォントサイズ変更を、表示メニューや設定と
  同じ扱いにした。設定に保存され、6〜32pt の範囲に収まる。
- SFTP クライアントのパネルに、使い方の案内を表示するようにした
  （ターミナルで機器へ SSH 接続すると使えるようになること、
  SSH で入れても機器が SFTP に対応していなければ使えないこと）。
- `nano` や `vi` のような全画面アプリを開いたときに、対応していない旨を
  ターミナルへ表示するようにした。これらは画面の位置を指定して描くため、
  表示が 1 行に潰れる。表示だけの問題で、機器との接続は切れていない。
  1 接続につき一度だけ出る。正しく表示できるようにするのは次の版で扱う。

## [1.1.0] - 2026-08-25

README に書いてあるのに実装が無かった機能を実装し、SNMPv3 を実際に使える
ようにしました。

### 追加
- **接続時の自動実行コマンド** — グループごとに登録したコマンドを接続直後に
  送る（例: `terminal length 0`）。デバイスツリーでグループを右クリックし
  「グループを編集」から設定する。SSH と Telnet が対象。`config.json` へ
  平文で保存されるため、パスワードは入れないこと。
- **設定ダイアログ**（ツール → 設定）— ターミナルの配色とフォント、
  SFTP クライアントの挙動、起動時の更新チェックを変更できる。
- **コピー / 貼り付け**（編集メニュー）— `Ctrl+Shift+C` / `Ctrl+Shift+V`。
  ターミナルの慣例に合わせ、`Ctrl+C` は機器への割り込み (0x03) 送信のまま
  残してある。貼り付けは接続中のタブでのみ働く。
- **フォントサイズの変更**（表示メニュー）— 6〜32pt。設定に保存される。
- SNMP GET / WALK の SNMPv3 認証（USM）。
  認証は MD5 / SHA-1 / SHA-224 / SHA-256 / SHA-384 / SHA-512、
  暗号化は DES / 3DES / AES-128 / AES-192 / AES-256
  （AES-192 / AES-256 はベンダー実装で広く使われている Reeder 方式）。
  認証情報は保存せず、起動のたびに入力する。
- SNMPv3 Trap の受信。送信元機器の EngineID を登録して使う。
  v1/v2c と v3 を同一ポートで同時に受信できる。
- Trap 一覧とエクスポートに「セキュリティ」列。
  受け取った Trap がどの版・どの保護レベルで届いたかが分かる。

### 変更
- Trap 受信を生 UDP ソケットから pysnmp のエンジンへ載せ替えた。
- **Trap のエクスポート（CSV / TXT）に「送信元ポート」と「セキュリティ」の
  列を追加した。CSV の列構成が変わるので、出力を機械処理している場合は
  確認すること。**
- デバイスツリーの右クリックメニューを「グループ名を変更」から
  「グループを編集」に改めた（名前以外も編集できるようになったため）。

### 修正
- ターミナルでコピーしてから貼り付けると、貼り付けた内容が本来の位置では
  なく、コピーのために選択していた場所へ上書き表示されていた。
  1回目が反映されていないように見えるため貼り付けを繰り返すと、
  機器へは同じコマンドが二重に送られていた。
  選択したまま機器がログを送ってくると選択が外れる問題も併せて修正。
- メニューにあるのに選んでも何も起きない項目が5つあった
  （ツール → 設定、編集 → コピー / 貼り付け、表示 → フォントサイズ拡大 / 縮小）。
- 貼り付けが再接続待機中のガードを迂回し、切れたセッションへ文字を
  送っていた。キーボード入力は正しく止まっていたが、貼り付けだけ通っていた。
- 設定ファイルのフォントサイズが壊れた値だと、フォントサイズの変更で
  落ちることがあった。
- 設定ダイアログを開いて何も変えずに OK を押すと、設定値が書き換わることが
  あった。
- SFTP クライアントで、表示フィルタに隠れているファイル（ドットファイルなど）
  が、上書き確認なしに上書きされていた。
- 設定ファイルが壊れているときに設定ダイアログを開けなかった。
- Trap のバージョンに v3 を選んでも、コミュニティ `public` の v1/v2c Trap を
  受理していた。
- v1 Trap のコミュニティ文字列が、画面と CSV / JSON / TXT エクスポートに
  そのまま出ていた。
- v3 のパスワードが短いとき、原因の分からないエラー（Trap 受信では
  「ポートで待ち受けできません」）になっていた。
- Trap 受信スレッドが終了しても、画面が「受信中」の表示のまま残っていた。
- Windows で受信ポートが他プロセスに奪われうる状態だった（排他バインド）。

## [1.0.0] - 2026-08-23

公開版としての最初のリリースです。

### 追加
- 配布物への第三者ライセンス表示（`THIRD-PARTY-NOTICES.txt`）。
  ビルド環境から `tools/gen_third_party_notices.py` が生成し、リリース ZIP へ同梱する。
  リリース本文には GPL-3.0 第6条にもとづく、対応するソースコードの入手先を記載する。
- SSH / Telnet / シリアル接続に対応したターミナル（複数タブ、デバイスのグループ管理）
- マクロ機能と接続時の自動実行コマンド
- セッションログの保存
- Syslog 受信（UDP / TCP、レベルフィルタ）
- SNMP Trap 受信（MIB による OID 解決、CSV エクスポート）
- TFTP サーバ / FTP サーバ（ネットワーク機器の config・イメージ授受、転送履歴）
- SFTP サーバおよび SFTP クライアント（双方向転送）
- ツールエリアのタブ化と、パネルの別ウィンドウ切り離し
- 受信サーバ起動時の Windows ファイアウォール受信許可ルールの自動追加
- SSH ホストキーの TOFU 検証
- GitHub Releases を参照する自動更新チェック
- 端末エスケープシーケンスの解釈
  - CSI（カーソル移動 `D`/`C`、行消去 `K`、画面消去 `J`）
  - DEC プライベートモード `ESC[?<n>h` / `ESC[?<n>l`（bash のブラケットペースト
    `ESC[?2004h` など）を画面へ出さずに読み飛ばす
  - OSC（`ESC]` ウィンドウタイトル等）および `ESC(B` / `ESC=` / `ESC>` の読み飛ばし。
    解釈できない列に当たっても走査位置が必ず前進するため、描画処理が停止しない

### セキュリティ
- パスワードの保存を **Windows DPAPI のみ**とした。暗号鍵は OS・ユーザーアカウントに紐付く。
  DPAPI が利用できない環境では暗号化せず、明示的にエラーとする
  （パスワードが平文で設定ファイルへ書き出される経路を持たない）。
- 復号できないパスワードを再暗号化しないようにした。DPAPI の鍵は環境に紐付くため、
  設定ファイルを別の PC やユーザーアカウントへ移すと復号に失敗する。その値を
  平文とみなして再度暗号化すると暗号文が二重に包まれ、元のパスワードが復元不能になる。
  保存は起動時にも走るため、原本を保護したうえで復号できなかった件数を通知する。
- 各サーバの既定ルートを専用フォルダ（`./tftp_root` / `./ftp_root` / `./sftp_root`）とした。
  TFTP はプロトコル上そもそも認証が無く `0.0.0.0` で待ち受けるため、
  個人フォルダを既定にすると、既定のまま起動しただけで公開されてしまう。
- SFTP サーバのユーザー名・パスワードに既定値を設けず、未入力では起動しないようにした。
- SFTP サーバのホストキーを `~/.netbelt/sftp_host_key` へ永続化した。起動のたびに
  生成すると毎回鍵が変わり、一度接続したクライアントが以後 host key mismatch で
  接続を拒否してしまう。生成はサーバ起動時に行い、アプリの起動を待たせない。
- 更新チェックで GitHub トークンの値をログへ出さないようにした。
- FTP サーバのユーザー名・パスワードに既定値を設けないようにした。
- SNMP Trap の「許可コミュニティ」を実際に照合するようにした。これまでは
  入力欄が保持・表示されるだけで照合しておらず、任意の送信元・任意のコミュニティの
  Trap をすべて受理していた。
- TFTP で RFC 1350 の転送 ID (TID) を照合するようにした。照合が無いと、同一
  セグメントの任意ホストが期待ブロック番号の DATA を1発送るだけで、進行中の
  アップロードへ任意データを混入させられる。あわせてパケット長を検証し、
  短いデータグラムで転送が中断しないようにした。
- 各サーバの待ち受けソケットで `SO_EXCLUSIVEADDRUSE` を使うようにした。
  Windows の `SO_REUSEADDR` は使用中ポートへの二重バインドを許すため、
  他のツールが使っているポートへ黙って割り込み、パケットの行き先が
  不定になる状態だった。衝突は起動時のエラーとして表面化する。
- SFTP サーバの `open()` が SFTP のフラグ（O_TRUNC / O_CREAT / O_EXCL）を
  尊重するようにした。読み書き両用で開いた瞬間に既存ファイルが消える問題と、
  読み取り専用ハンドルが書き込み可能として応答する問題を修正。
  読み取り目的の open で空ディレクトリを作らないようにした。

- 自動更新でダウンロードした ZIP の SHA-256 を照合するようにした。リリースには
  `.sha256` を併せて添付し、一致しない場合とチェックサムを取得できない場合は
  更新を中止して一時ファイルを削除する。破損の検知が目的であり、GitHub 自体が
  侵害された場合の改ざんは検知できない。

- SFTP / TFTP のルート拘束を `realpath` ベースに改めた。`abspath` は `..` を畳むだけで
  シンボリックリンクやジャンクションを解決しないため、ルート配下にリンクを1つ
  置かれるだけでその先の任意の場所を読み書きできた。Windows のジャンクションは
  一般ユーザーでも作成できるため、実際に踏める経路だった。

- 更新ファイルは検証が通るまで `.part` 名で保存し、SHA-256 の照合後に `.zip` へ
  改名するようにした。あわせて検証済みの証をハッシュの控えとして残し、適用前に
  再確認する。これまでは検証前のファイルを最終名で書いていたため、ダウンロードを
  中断すると未検証の部分ファイルが「未適用の更新」として拾われ、検証を経ずに
  適用できる経路があった。
- ダウンロードの中止を `terminate()` ではなく協調的なキャンセルに改めた。
  `terminate()` は任意の位置でスレッドを止めるため、壊れたファイルを残していた。
- SNMP Trap の許可コミュニティを、値ではなく件数でログに記録するようにした。
  コミュニティ文字列は SNMPv1/v2c の合言葉であり、凍結ビルドではログが
  恒久保存されるため。

### 修正
- 更新の確認が通信エラーで失敗したときに「最新です」と表示していた問題を修正。
  手動確認では失敗を知らせ、起動時の自動確認では記録に留める。
- `requirements.txt` の `pysnmp` / `pyasn1` / `requests` をバージョン固定した。
  浮動指定のままでは再ビルド時に構成が変わり、配布したバイナリを
  同じ内容で再現できなくなるため。
- アプリ終了時に SNMP Trap 受信スレッドを停止するようにした。
- SNMP のワーカースレッドを、実行中に参照を落として破棄しないようにした。
  `terminate()` の使用もやめ、待機にはタイムアウトを設けた。
- 更新ダイアログの `DownloadThread` がチェックサムの URL を受け取れず、
  更新のダウンロードが実行時に失敗する問題を修正。
- 接続ワーカースレッドから GUI ウィジェットを直接操作していた箇所を、
  シグナル経由へ改めた（SSH / シリアル / Telnet の3種）。接続に失敗するたびに
  必ず通る経路で、Qt のウィジェットを GUI スレッド外から触っていた。
- SFTP サーバと SNMP Trap 受信で、ポートが使用中でも「実行中」と表示され、
  実際には何も待ち受けていない状態になる問題を修正。バインドを起動処理の中で
  行い、成否を戻り値へ反映するようにした。

[1.1.1]: https://github.com/gomanizm/NetBelt/releases/tag/v1.1.1
[1.1.0]: https://github.com/gomanizm/NetBelt/releases/tag/v1.1.0
[1.0.0]: https://github.com/gomanizm/NetBelt/releases/tag/v1.0.0
